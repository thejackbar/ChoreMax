# Database
DB_USER=choremax
DB_PASSWORD=changeme_use_a_strong_password
DB_NAME=choremax

# JWT — generate with: python3 -c "import secrets; print(secrets.token_hex(64))"
JWT_SECRET=
JWT_EXPIRY_DAYS=7

# CORS — frontend origin(s), comma-separated
FRONTEND_ORIGIN=https://choremax.bltbox.com

# Environment
NODE_ENV=production
